<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="cn.edu.gxu.gxucpcsystem.dao.mysql.PlayerDao">
    <insert id="addPlayer">
        insert into information(userName,
                                informationId,
                                userSex,
                                userCourse,
                                userClass,
                                userQQ,
                                userMail,
                                contestId,
                                star,
                                `group`,
                                userPhone,
                                userId)
        values (#{userName},
                #{informationId},
                #{userSex},
                #{userCourse},
                #{userClass},
                #{userQQ},
                #{userMail},
                #{contestId},
                #{star},
                #{group},
                #{userPhone},
                #{userId})
    </insert>
    <select id="queryByPage" resultType="cn.edu.gxu.gxucpcsystem.domain.Player">
        select *
        from gxucpc.information
        WHERE contestId = #{contestId}
        limit #{offset}, #{count}
    </select>
    <select id="getCount" resultType="java.lang.Integer">
        select count(*)
        from gxucpc.information
    </select>
    <select id="queryByPageRegex" resultType="cn.edu.gxu.gxucpcsystem.domain.Player">
        select *
        from gxucpc.information
        WHERE contestId = #{contestId}
          and (
                    userName like '%#{regex}%' or
                    informationId like '%#{regex}%' or
                    userSex like '%#{regex}%' or
                    userCourse like '%#{regex}%' or
                    userClass like '%#{regex}%' or
                    userQQ like '%#{regex}%' or
                    userMail like '%#{regex}%' or
                    contestId like '%#{regex}%' or
                    star like '%#{regex}%' or
                    `group` like '%#{regex}%' or
                    userPhone like '%#{regex}%' or
                    userId like '%#{regex}%')
        limit #{offset}, #{count}
    </select>
    <select id="regexCount" resultType="java.lang.Integer">
        select count(*)
        from gxucpc.information
        where contestId = #{contestId}
          and (
                    userName like '%#{regex}%' or
                    informationId like '%#{regex}%' or
                    userSex like '%#{regex}%' or
                    userCourse like '%#{regex}%' or
                    userClass like '%#{regex}%' or
                    userQQ like '%#{regex}%' or
                    userMail like '%#{regex}%' or
                    contestId like '%#{regex}%' or
                    star like '%#{regex}%' or
                    `group` like '%#{regex}%' or
                    userPhone like '%#{regex}%' or
                    userId like '%#{regex}%')
    </select>
    <update id="updatePlayer">
        update information
        set userName   = #{userName},
            userSex    = #{userSex},
            userCourse = #{userCourse},
            userClass  = #{userClass},
            userQQ     = #{userQQ},
            userMail   = #{userMail},
            contestId  = #{contestId},
            star       = #{star},
            `group`    = #{group},
            userPhone  = #{userPhone},
            userId     = #{userId}
        where informationId = #{informationId}
    </update>
    <delete id="deletePlayer">
        delete
        from information
        where informationId = #{informationId};
    </delete>
</mapper>